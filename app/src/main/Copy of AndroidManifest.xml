<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.test">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logophinma"
        android:label="Agile Prototype"
        android:roundIcon="@drawable/logophinma"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test"
        tools:targetApi="31">
        <activity
            android:name=".aboutUs"
            android:exported="false" />
        <!-- DELETEABLE ACTIVITY -->
        <activity android:name=".MainActivity" />
        <activity
            android:name=".SubActMyHabits"
            android:exported="false" />
        <activity
            android:name=".SubActMyTasks"
            android:exported="false" />
        <activity
            android:name=".SubActMyGoals"
            android:exported="false" /> <!-- CHANGED FROM .MAINACTIVITY TO SPLASHSCREEN -->
        <activity
            android:name=".splashscreen"
            android:exported="true"
            android:theme="@style/splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CalendarActivity" />
    </application>

</manifest>